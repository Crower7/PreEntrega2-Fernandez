import React from 'react'
import logo from '../Img/logoCarrito.png'

const CartWidget = () => {
  return (
    <div>
        <img src={logo} alt='logo' className='logo'/>
        {0}
    </div>
  )
}

export default CartWidget